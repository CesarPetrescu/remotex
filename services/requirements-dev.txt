-r requirements.txt
# Pinned to one minor: ruff adds/changes rules in every minor release, so
# an open range turns CI red on code nobody touched (0.16.2 did exactly
# that with F401). Patches are safe; bump the minor deliberately.
ruff>=0.16.2,<0.17
pytest>=8
pytest-asyncio>=0.23
pytest-aiohttp>=1.0
